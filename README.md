# SquareLauncher
SquareLauncher is a free MC launcher that has a basic Tkinter UI.
# Setup
MacOS for now.<br>
```git clone https://github.com/thedistromaker/SquareLauncher.git```<br>
```curl -fsSL https://raw.githubusercontent.com/thedistromaker/SquareLauncher/main/install.sh | sudo bash```
